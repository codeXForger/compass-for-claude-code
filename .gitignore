node_modules
dist
.DS_Store
*.log
.env
.env.local
.vite
.tsbuildinfo

# npm pack output
*.tgz

# Runtime backups written by writeWithBackup() before config edits
.claude/.compass-backups/

# Orphaned temp files left by interrupted atomic writes (writeWithBackup)
.claude/*.tmp.*
